function printinputs(startFreq, stopFreq, definePoints, numMeasurements, fileName, numAntennas, ports, waitTime)
    disp(startFreq)
    disp(stopFreq)
    disp(definePoints)
    disp(numMeasurements)
    disp(fileName)
    disp(numAntennas)
    disp(ports)
    disp(waitTime)
end

